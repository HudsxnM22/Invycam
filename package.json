{
  "name": "invycam",
  "version": "1.0.0",
  "description": "",
  "main": "public/main.js",
  "homepage": "./",
  "scripts": {
    "react-start": "react-scripts start",
    "react-build": "react-scripts build",
    "electron": "electron .",
    "electron-dev": "cross-env NODE_ENV=development concurrently \"npm run react-start\" \"wait-on http://localhost:3000 && electron .\"",
    "electron-prod": "cross-env NODE_ENV=production npm run react-build && electron .",
    "electron-pack": "npm run react-build && electron .",
    "build": "npm run react-build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^38.1.2",
    "wait-on": "^8.0.3"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "ldrs": "^1.1.7",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-draggable": "^4.4.3",
    "react-hook-form": "^7.65.0",
    "react-scripts": "^5.0.1",
    "uiohook-napi": "^1.5.4",
    "zustand": "^5.0.7"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
