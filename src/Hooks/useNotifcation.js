import { create } from "zustand";

const useNotification = (set) => ({
    notifications: [],

    notify: (type, message) => {
        const id = Date.now()
        
    }
})

export default useNotifcation